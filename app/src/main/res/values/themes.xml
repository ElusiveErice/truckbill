<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.Truckbill" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.Truckbill" parent="Base.Theme.Truckbill">
        <!-- Transparent system bars for edge-to-edge. -->
        <!--        <item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--        <item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>-->
    </style>

    <style name="Theme.MyDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowTranslucentStatus">true</item><!--透明状态栏并占用状态栏位置 -->
        <item name="android:windowIsTranslucent">true</item><!-- 半透明 -->

        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowIsFloating">false</item><!-- 浮现在Activity之上 -->
        <item name="windowNoTitle">true</item><!-- 无标题 -->
    </style>
</resources>